import React from 'react';
import { render } from '@testing-library/react';
import App from './App';
import { Item } from 'semantic-ui-react';

describe('Testing the customer form',()=>{
    it('should submit on submit button press', ()=> {

    })

    it('should clear on clear button press', ()=> {

    })

    it('should show message on submit button press', ()=> {

    })
})